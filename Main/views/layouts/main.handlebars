<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hollywood Movie Night</title>
    <link rel="stylesheet" href="/css/jass.css">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            background-color: #000000;
            color: #FFFFFF;
            font-family: 'Arial Black', Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        .container {
            display: flex;
            flex-direction: column;
            height: 100vh;
            justify-content: space-between;
            border-radius: 15px; 
            overflow: hidden; 
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem;
            background-color: #DC143C;
            color: #FFFFFF;
        }

        nav {
            display: flex;
            gap: 1rem;
        }

        main {
            flex-grow: 1;
            padding: 2rem;
            background-color: #111111;
        }

        footer {
            background-color: #DC143C;
            padding: 1rem 0;
            text-align: center;
            color: #FFFFFF;
        }

        h1 {
            font-size: 36px;
            margin: 0;
            display: flex;
            align-items: center;
        }

        a {
            text-decoration: none;
            color: #FFFFFF;
        }

        button {
            background-color: transparent;
            border: none;
            color: #FFFFFF;
            cursor: pointer;
            font-weight: bold;
            font-size: 18px;
        }

        button:hover {
            text-decoration: underline;
        }

        .emoji {
            margin: 0 10px; 
        }

    </style>
</head>
<body>
<div class="container">
    <header>
        <h1>
            <span class="emoji" role="img" aria-label="money">🎬</span>
            <a href="/">Hollywood Movie Night</a>
            <span class="emoji" role="img" aria-label="money">🎬</span>
        </h1>
        <nav>
            <!-- Conditionally render login or logout links -->
            {{#if logged_in}}
            <a href="/profile">Profile</a>
            <button class="no-button" id="logout">Logout</button>
            {{else}}
            <a href="/login">Login</a>
            {{/if}}
        </nav>
    </header>
    <main class="container-fluid mt-5">
        <!-- Render the sub layout -->
        {{{ body }}}
    </main>
    <footer>
        &copy; 2024 Hollywood Movie Night
    </footer>
</div>

<!-- Render script for logged-in users only -->
{{#if logged_in}}
<script src="/js/logout.js"></script>
{{/if}}
</body>
</html>
